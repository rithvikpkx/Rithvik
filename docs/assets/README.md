# README assets

Media referenced by the project root `README.md`.

Expected files (placeholders — add manually):

- `demo.mp4` — looping, muted screen recording of the site in action (used in the Demo section)
- `demo.gif` — optional GIF fallback for the demo

Keep files reasonably small so the repo stays light — compress recordings before committing.
